# Right Step API — Environment Configuration
# IMPORTANT: Rename this file to .env on the server (public_html/api/.env)
# The .env file is protected by .htaccess and will NOT be accessible from the web.

# Database (create in Hostinger panel > Databases > MySQL)
DB_HOST=localhost
DB_NAME=
DB_USER=
DB_PASS=

# JWT Secret (pre-generated — keep secret)
JWT_SECRET=2e5344a6187cb951c73f636b414cfd5793c9713332b1bc6aa77dde0b789ecc03

# SMTP (create email account in Hostinger panel > Emails)
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_USER=noreply@rightstepae.com
SMTP_PASS=
